# #912 class outfit colors evidence

`receipt.json` is intentionally pending. It records no screenshots, requests,
UUIDs, observations, or verdicts until the controller performs the normal-game
human run.

Use `node scripts/captureClassOutfitColorEvidence.mjs <receipt.json>` after that
run. A captured receipt must include real creation/session screenshots, owner
and peer matching observations, movement, hand-equipment witnesses, stable
material identities, and zero HTTP/application failures. Do not replace this
pending receipt with synthetic observations.
