# #897 all-race hair customization web evidence

Status: **accepted** — Kirk: “it really does”.

This evidence exercises the normal character-creation and session routes against the exact merged provider authority. The Human flow opened the production picker, loaded all 38 scalp and 18 facial-hair options, selected Hair 38 + Facial Hair 18 with `#64A5CE` and roughness `0.55`, sent one response-authoritative Apply, displayed the returned summary, and finalized the character.

Two real Reference Tomb encounters then covered all eight profiles:

1. Human, Elf, Gnome, and Tiefling.
2. Dwarf, Half-Elf, Halfling, and Half-Orc.

Every active body and expected default/explicit accessory request returned successfully. Browser capture observed zero HTTP asset failures and zero application failures. Owner `Appearance.hair` and peer roster `Customization.hair` are additionally parameterized through the same renderer in the automated session suite.

Files:

- `human-picker-selected.png` — production Human picker with explicit styles/color/roughness.
- `human-apply-summary.png` — response-authoritative draft summary after Apply.
- `session-human-elf-gnome-tiefling.png` — first four-profile normal encounter.
- `session-dwarf-halfelf-halfling-halforc.png` — second four-profile normal encounter.
- `receipt.json` — exact provider/web authority, request observations, verdict, and screenshot hashes.

Licensed runtime GLBs and thumbnails remain ignored under `public/models/`; none are tracked by this web repository.
