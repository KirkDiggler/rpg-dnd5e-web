# Example .env.local file for development

# API Configuration
VITE_API_URL=http://localhost:50051

# Add this to your shell profile for push reminders:
# alias gp="echo '⚠️  Running CI checks first...' && npm run ci-check && git push"
# alias gpu="echo '⚠️  Running CI checks first...' && npm run ci-check && git push -u origin HEAD"