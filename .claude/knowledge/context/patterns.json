[
  {
    "type": "pattern",
    "id": "serialized-refresh-with-trailing-invalidation",
    "created": "2026-08-26",
    "updated": "2026-08-26",
    "status": "active",
    "name": "Serialize snapshot refreshes with a trailing invalidation pass",
    "description": "When a stream event invalidates data while its unary snapshot read is already in flight, sharing only the current promise can publish a pre-event snapshot and lose the invalidation. Record a pending bit/key set, let the active read settle, then immediately run one coalesced trailing read. Fence timer, flush, response, and trailing work by lifecycle/key generation; authoritative replacement or key change cancels the pending pass.",
    "example": "useCoalescedSessionRefreshes serializes callback passes and useCharacterData loops once more when refetch marks its active read invalidated.",
    "anti_pattern": "Returning the current in-flight promise without recording that a newer event arrived, or launching an overlapping refresh that permits reversed responses.",
    "domain": "web/session-query-reconciliation",
    "references": [
      "src/components/session/useCoalescedSessionRefreshes.ts",
      "src/api/useCharacterData.ts",
      "rpg-dnd5e-web#822"
    ]
  },
  {
    "type": "pattern",
    "id": "separate-last-good-display-from-execution-freshness",
    "created": "2026-08-26",
    "updated": "2026-08-26",
    "status": "active",
    "name": "Separate last-good display from execution freshness",
    "description": "For server-authored command declarations, retaining the last successful values improves continuity, but those values must not remain dispatch authority after an event, refetch, or error. Expose a separate fresh flag and synchronous invalidate operation. Revoke freshness before scheduling reconciliation; restore it only on a successful response for the newest current key/generation.",
    "example": "useSessionTurn/useSessionAfford retain old display while fresh=false; SessionEncounterView synchronously invalidates both on every delivered sequence, and CombatExperience disables Attack, Move preview/dispatch, and End Turn until both are fresh and coherent.",
    "anti_pattern": "Treating last-good values as executable after background refresh begins/fails, or clearing useful display just to fail closed.",
    "domain": "web/session-action-authority",
    "references": [
      "src/api/useSessionTurn.ts",
      "src/api/useSessionAfford.ts",
      "src/components/session/SessionEncounterView.tsx",
      "rpg-dnd5e-web#822"
    ]
  },
  {
    "type": "pattern",
    "id": "reconcile-ambiguous-mutation-outcomes-without-replay",
    "created": "2026-08-26",
    "updated": "2026-08-26",
    "status": "active",
    "name": "Fail closed and reconcile ambiguous mutation outcomes without replay",
    "description": "A successful command response and a non-precondition transport failure can both mean server state changed before an event arrives. Revoke old execution authority synchronously, preserve honest presentation facts, and queue serialized authoritative reads. Never automatically replay a mutation whose commit status is unknown.",
    "example": "Move acceptance invalidates Turn/Afford before animation; non-FAILED_PRECONDITION Attack and EndTurn failures clear selection, retain their error, invalidate authority, and schedule the same authoritative reconciliation as success.",
    "anti_pattern": "Leaving old declarations executable until a stream event arrives, or retrying an Attack/EndTurn after an unavailable/unknown response.",
    "domain": "web/session-command-recovery",
    "references": [
      "src/components/session/SessionEncounterView.tsx",
      "src/components/session/combat-experience/useSessionCombatExperience.ts",
      "src/components/session/useSessionWalk.ts",
      "rpg-dnd5e-web#822"
    ]
  },
  {
    "type": "pattern",
    "id": "gate-r3f-render-witnesses-by-renderer-frame-advancement",
    "created": "2026-08-27",
    "updated": "2026-08-27",
    "status": "active",
    "name": "Gate R3F rendered-pose witnesses by renderer frame advancement",
    "description": "React Three Fiber useFrame callbacks run before the renderer draws the mutations applied in that callback. For completion, settlement, or rendered-attachment truth, retain the applied pose with gl.info.render.frame and emit its witness only when a later frame observes a greater renderer frame counter. Fence the pending witness to callback, die, role, and generation identity so rerenders or replaced captures cannot emit stale facts.",
    "example": "RuntimeDiceMesh exposes onFrameDrawn for settlement and onPoseDrawn for held attachment after renderer frame advancement; RollGroupRuntimeMember publishes target and grabbed-point witnesses only through those seams.",
    "anti_pattern": "Treating a pose mutation or a useFrame callback as proof that the user-visible frame was rendered.",
    "domain": "web/r3f-render-lifecycle",
    "references": [
      "src/components/ui/dice/RuntimeDiceMesh.tsx",
      "src/components/ui/dice/RollGroupRuntimeMember.tsx",
      "rpg-dnd5e-web#826"
    ]
  },
  {
    "type": "pattern",
    "id": "fence-r3f-instance-lifecycle-from-treatment-and-strict-probes",
    "created": "2026-09-01",
    "updated": "2026-09-01",
    "status": "active",
    "name": "Fence R3F instance lifecycle from treatment updates and StrictMode probe evidence",
    "description": "Keep clone/bind/add/remove/dispose effects dependent only on asset and mount identity. Read volatile renderer callbacks and presentation treatments through latest refs, then mutate instance-owned materials in a separate effect. In StrictMode, a child's second passive setup can report before its parent's identity-restoring setup; if that terminal report is fenced, retry it in a microtask only while the exact second mounted instance is still current so diagnostics cannot retain UUIDs from the disposed probe instance.",
    "example": "SkinnedAccessoryAttachment retains mesh/material UUIDs across color/PBR updates, keeps the latest R3F invalidate callback behind a ref, and retries only the still-mounted StrictMode attachment's terminal status.",
    "anti_pattern": "Putting treatment values or a volatile useThree callback in the clone/bind lifecycle dependency list, or relaxing stale-identity fencing to make StrictMode's second terminal report pass.",
    "domain": "web/r3f-render-lifecycle",
    "references": [
      "src/components/hex-grid/SkinnedAccessoryAttachment.tsx",
      "src/components/hex-grid/SkinnedAccessoryAttachment.test.tsx",
      "rpg-dnd5e-web#881"
    ]
  }
]
